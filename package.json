{
  "name": "personal-social-video-downloader",
  "version": "1.5.1",
  "private": true,
  "description": "Personal tracker-free X and Instagram WebExtension.",
  "scripts": {
    "check": "node --check background.js && node --check page-hook.js && node --check content.js && node --check popup.js && node --check audio.js"
  }
}
