# Optional local MP3 encoder

`vendor/lamejs.iife.js` is intentionally not fetched at runtime. Run `setup-mp3.ps1` (Windows) or `setup-mp3.sh` once to install the LGPL-3.0 `@breezystack/lamejs` encoder locally. No remote code is loaded by the extension at runtime.
